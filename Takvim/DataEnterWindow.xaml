<UserControl x:Class="Takvim.DataEnterWindow" x:Name="UserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
    xmlns:TwainControl="clr-namespace:TwainControl;assembly=TwainControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Takvim"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    Margin="2"
    d:DataContext="{d:DesignInstance Type={x:Type local:Data}}"
    d:DesignHeight="300" d:DesignWidth="420" Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded" UseLayoutRounding="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <local:StringNullableToBooleanConverter x:Key="StringNullableToBooleanConverter" />
        <Color x:Key="GlyphColor">#FF444444</Color>
        <local:NullableToBooleanConverter x:Key="NullableToBooleanConverter" />
        <CollectionViewSource x:Key="FilteredCvs" Source="{StaticResource XmlData}" />
    </UserControl.Resources>
    <Microsoft_Windows_Themes:SystemDropShadowChrome>
        <GroupBox>
            <GroupBox.Header>
                <UniformGrid Columns="2">
                    <TextBlock>
                        <Run Text="VERİ GİR" />
                        <Run Text="{Binding TamTarih, StringFormat=\{0:d\}}" Language="tr-tr" />
                    </TextBlock>
                    <TextBlock HorizontalAlignment="Right" FontSize="15" FontFamily="Segoe UI Black">
                        <Hyperlink Command="{Binding PencereKapat}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
                            <Run Text="X" />
                        </Hyperlink>
                    </TextBlock>
                </UniformGrid>
            </GroupBox.Header>
            <GroupBox.Style>
                <Style BasedOn="{StaticResource {x:Type GroupBox}}" TargetType="{x:Type GroupBox}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding GünAdı}" Value="Cmt">
                            <Setter Property="Background" Value="{Binding Owner.DataContext.SeçiliRenkCmt, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding GünAdı}" Value="Paz">
                            <Setter Property="Background" Value="{Binding Owner.DataContext.SeçiliRenkPaz, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Converter={StaticResource GünTatilConverter}, Mode=OneWay}" Value="true">
                            <Setter Property="Background" Value="{Binding Owner.DataContext.ResmiTatilRenk, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Converter={StaticResource GünTatilConverter}, Mode=OneWay}" Value="{x:Null}">
                            <Setter Property="Background" Value="{Binding Owner.DataContext.ResmiTatilRenk, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding TamTarih, Converter={StaticResource DatePassedBooleanConverter}}" Value="true">
                            <Setter Property="Background" Value="Gray" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </GroupBox.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1.5*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBox Text="{Binding GünNotAçıklama, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                    Grid.RowSpan="2"
                    AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible" />
                <Grid Grid.Column="1">
                    <Image Source="{Binding ResimData, ConverterParameter=96, Converter={StaticResource WebpByteArrayToImageConverter}, Mode=OneWay}" />
                    <ContentControl IsEnabled="{Binding ResimData, Converter={StaticResource NullableToBooleanConverter}, Mode=OneWay}"
                        Width="16" Height="16"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Template="{StaticResource Eye}">
                        <ContentControl.ToolTip>
                            <ToolTip Template="{StaticResource GreenToolTipControlTemplate}">
                                <Grid>
                                    <Image Source="{Binding ResimData, Converter={StaticResource WebpByteArrayToImageConverter}, Mode=OneWay}" />
                                    <Viewbox Width="200" Height="200">
                                        <TextBlock Text="{Binding Boyut, StringFormat=\{0\} KB}" Foreground="Red" />
                                    </Viewbox>
                                </Grid>
                            </ToolTip>
                        </ContentControl.ToolTip>
                    </ContentControl>
                </Grid>
                <Slider Grid.Row="1" Grid.Column="1"
                    AutoToolTipPlacement="TopLeft" IsSnapToTickEnabled="True" IsTabStop="False"
                    LargeChange="1" Maximum="100" Minimum="1"
                    SmallChange="1" TickFrequency="1"
                    ToolTip="WEBP RESİM YÜKLEME KALİTESİ 1 EN DÜŞÜK BOYUTTUR. DEĞERİ DEĞİŞTİRİP DOSYAYI TEKRAR YÜKLEYİP ÖNİZLEME GÖZÜNDEN BAKIN"
                    Value="{Binding WebpQuality, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <UniformGrid Grid.Row="2" Grid.ColumnSpan="2" Columns="3" Rows="1">
                    <CheckBox Content="ÖNEMLİ Mİ" Foreground="Red" VerticalAlignment="Center" IsChecked="{Binding ÖnemliMi, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
                    <CheckBox Content="AY TEKRAR" VerticalAlignment="Center" IsChecked="{Binding AyTekrar, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" ToolTip="BU İŞARETLENİRSE HER AYIN AYNI GÜNÜNE İŞARET VERİLİR" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource Renkler}}"
                        ItemContainerStyle="{StaticResource ComboBoxItemContainerStyle}"
                        ItemTemplate="{StaticResource ComboBoxRectItemTemplate}"
                        ItemsPanel="{StaticResource CbItemsPanelTemplate}"
                        ScrollViewer.VerticalScrollBarVisibility="Disabled"
                        SelectedValue="{Binding VeriRenk, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Name" ToolTip="Veri Renk" />
                </UniformGrid>
                <Slider Grid.Row="3" Grid.ColumnSpan="2"
                    AutoToolTipPlacement="TopLeft" AutoToolTipPrecision="1" IsSnapToTickEnabled="True"
                    LargeChange="0.5" Maximum="99" Minimum="0"
                    SmallChange="0.5" TickFrequency="0.5" ToolTip="ETKİNLİK SÜRESİ SAAT"
                    Value="{Binding EtkinlikSüresi, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
                <local:TimePicker Grid.Row="4" Grid.ColumnSpan="2"
                    ItemsSource="{Binding Saatler}"
                    ClearButtonVisible="Visible"
                    TimeValue="{Binding SaatBaşlangıç, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="RESİM YÜKLE" Grid.Row="5" Grid.ColumnSpan="2" Command="{Binding ResimYükle, Mode=OneWay}">
                    <Button.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Mode=Self}}">
                            <MenuItem Header="RESİM İPTAL" Command="{Binding Resimİptal}" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                <local:FileSelectorTextBox Grid.Row="6" Grid.ColumnSpan="2" IsEnabled="{Binding GünNotAçıklama, Converter={StaticResource StringNullableToBooleanConverter}}" Dosyalar="{Binding Dosyalar, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"
                    IsEnabled="{Binding ResimData, Converter={StaticResource NullableToBooleanConverter}, Mode=OneWay}"
                    Command="{Binding OcrUygula}"
                    CommandParameter="{Binding ResimData}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="OCR UYGULA" />
                        <ProgressBar Width="96" IsIndeterminate="{Binding OcrSürüyor}" />
                        <ContentControl IsEnabled="{Binding OcrMetin, Converter={StaticResource NullableToBooleanConverter}}"
                            Width="16" Height="16"
                            Template="{StaticResource Eye}"
                            ToolTip="{Binding OcrMetin}" />
                    </StackPanel>
                </Button>
                <Button Content="VERİYİ EKLE"
                    Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                    Command="{Binding XmlVeriEkle, Mode=OneWay}" />
                <ToggleButton x:Name="Tb" Grid.Row="8" Grid.Column="2">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                                        <TextBlock Text="TARAYICI" />
                                        <Path HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" RenderTransformOrigin="0.5,0.5">
                                            <Path.RenderTransform>
                                                <TransformGroup>
                                                    <ScaleTransform />
                                                    <SkewTransform />
                                                    <RotateTransform Angle="-90" />
                                                    <TranslateTransform />
                                                </TransformGroup>
                                            </Path.RenderTransform>
                                            <Path.Fill>
                                                <SolidColorBrush Color="{StaticResource GlyphColor}" />
                                            </Path.Fill>
                                        </Path>
                                    </StackPanel>
                                    <Popup x:Name="Popup"
                                        AllowsTransparency="True"
                                        IsOpen="{Binding IsChecked, ElementName=Tb}"
                                        Placement="Right"
                                        PlacementTarget="{Binding ElementName=Tb}"
                                        PopupAnimation="Slide" StaysOpen="False">
                                        <Microsoft_Windows_Themes:SystemDropShadowChrome>
                                            <ScrollViewer Padding="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                                <TwainControl:TwainCtrl Width="540" Height="465" Background="White" PropertyChanged="TwainCtrl_PropertyChanged" />
                                            </ScrollViewer>
                                        </Microsoft_Windows_Themes:SystemDropShadowChrome>
                                    </Popup>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsOpen, Converter={local:BoolInverter}, ElementName=Popup, Mode=OneWay}" Value="true">
                                    <Setter Property="IsHitTestVisible" Value="true" />
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <ListBox Grid.Column="2" Grid.RowSpan="8" ItemsSource="{Binding Mode=OneWay, Source={StaticResource FilteredCvs}}" ItemContainerStyle="{StaticResource ListBoxToolTipItemContainerStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontSize="12" FontFamily="Segoe UI Black">
                                        <Hyperlink x:Name="Hyp" Command="{Binding DataContext.XmlVeriSil, ElementName=UserControl}" CommandParameter="{Binding XPath=@Id}">
                                            <Run Text="X" />
                                        </Hyperlink>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="{Binding XPath=@SaatBaslangic}" />
                                        <Run Text="{Binding XPath=Aciklama}" />
                                    </TextBlock>
                                    <ContentControl IsTabStop="False" Template="{Binding Mode=OneWay, Source={StaticResource TimePanel}}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel.DataContext>
                                        <local:Data />
                                    </StackPanel.DataContext>
                                    <local:TimePicker ItemsSource="{Binding Saatler}" TimeValue="{Binding SaatBaşlangıç, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
                                    <local:MaskedTextBox Width="35"
                                        IncludePrompt="False" Mask="00" PromptChar="_"
                                        ValueType="{x:Type system:Double}"
                                        Value="{Binding EtkinlikSüresi, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <CheckBox Content="AY TEKRAR" VerticalAlignment="Center" IsChecked="{Binding AyTekrar}" />
                                    <Button Content="P"
                                        Command="{Binding XmlVeriGüncelle, Mode=OneWay}"
                                        CommandParameter="{Binding CommandParameter, ElementName=Hyp}"
                                        FontFamily="Wingdings 3"
                                        ToolTip="GÜNCELLE" />
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </Grid>
        </GroupBox>
    </Microsoft_Windows_Themes:SystemDropShadowChrome>
</UserControl>